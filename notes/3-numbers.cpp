/*
    Numbers
    - Operations:
      - sum: +
      - subtract: -
      - multiply: *
      - division: /
      - module: %
      - exponentiation: pow() from the cmath library (** is not valid)
*/
#include <iostream>

int main() {

    // Declare an integer
    int x = 2;

    // Declare a double
    double y = 1.3;

    // Sum
    std::cout << x+y;

    return 0;
    
}